name: coinswapr
description: Centralized crypto swapping

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.6 <3.0.0'

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  otp_text_field: ^1.1.3
  transparent_image: ^2.0.1
  equatable: ^2.0.5
  shared_preferences: ^2.0.20
  syncfusion_flutter_charts: ^21.1.35
  get_it: ^7.2.0
  adaptive_theme: ^3.2.0
  flutter_cache_manager: ^3.3.0
  provider: ^6.0.5
  http: ^0.13.5
  web_socket_channel: ^2.3.0
  fl_chart: ^0.61.0
  google_fonts: ^4.0.3
  pretty_qr_code: ^2.0.3
  flutter_riverpod: ^2.4.3
  hexcolor: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
   - assets/
